'use strict'
import path from '../path'
import {x as pointX, y as pointY} from './point'

export default () => {
  const x0 = pointX
  const x1 = null
  const y0 = 0
  const y1 = pointY
  const defined = true
  const context = null
  const output = null

  const area = data => {

  }
  return (x0, x1, y0, y1, defined, context, output, area, path)
}
